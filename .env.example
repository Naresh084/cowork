# ==============================================================================
# GeminiCowork Environment Configuration
# ==============================================================================
#
# Copy this file to .env and fill in your credentials.
# These values are used by the sidecar for OAuth authentication with external
# services. Credentials are stored securely in the system keychain.
#
# See docs/oauth-setup/ for detailed instructions on registering OAuth
# applications with each provider.
#
# ==============================================================================

# ------------------------------------------------------------------------------
# Google OAuth (for Google Workspace connector)
# ------------------------------------------------------------------------------
# Register at: https://console.cloud.google.com/apis/credentials
#
# Steps:
# 1. Create a new project or use existing one
# 2. Enable required APIs: Gmail API, Calendar API, Drive API, etc.
# 3. Configure OAuth consent screen (External or Internal)
# 4. Create OAuth 2.0 Client ID credentials (Desktop app type)
# 5. Copy the Client ID and Client Secret below
#
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# ------------------------------------------------------------------------------
# Microsoft OAuth (for Microsoft 365, Teams, Outlook connectors)
# ------------------------------------------------------------------------------
# Register at: https://portal.azure.com/#view/Microsoft_AAD_RegisteredApps
#
# Steps:
# 1. Register a new application
# 2. Set "Supported account types" to "Accounts in any organizational directory"
# 3. Add API permissions: Mail.ReadWrite, Calendars.ReadWrite, Files.ReadWrite
# 4. Enable "Allow public client flows" (for device code flow)
# 5. Copy the Application (client) ID below
#
# Note: No client secret needed - uses device code flow for better UX
#
MICROSOFT_CLIENT_ID=

# ------------------------------------------------------------------------------
# GitHub OAuth (for GitHub connector)
# ------------------------------------------------------------------------------
# Register at: https://github.com/settings/developers
#
# Steps:
# 1. Click "New OAuth App"
# 2. Set Homepage URL to http://localhost
# 3. Set Authorization callback URL to http://localhost:49152/oauth/callback
#    (or any available port in range 49152-65535)
# 4. Register the application
# 5. Copy the Client ID and generate a Client Secret
#
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# ------------------------------------------------------------------------------
# Slack OAuth (for Slack connector)
# ------------------------------------------------------------------------------
# Register at: https://api.slack.com/apps
#
# Steps:
# 1. Click "Create New App" > "From scratch"
# 2. Add required OAuth scopes under "OAuth & Permissions":
#    - chat:write, channels:read, channels:history, users:read
#    - files:read, files:write (if file access needed)
# 3. Install to your workspace
# 4. Copy the Client ID and Client Secret from "Basic Information"
#
SLACK_CLIENT_ID=
SLACK_CLIENT_SECRET=

# ------------------------------------------------------------------------------
# Linear OAuth (for Linear connector)
# ------------------------------------------------------------------------------
# Register at: https://linear.app/settings/api
#
# Steps:
# 1. Click "Create new OAuth application"
# 2. Set callback URL to http://localhost:49152/oauth/callback
# 3. Select required scopes: read, write, issues:create
# 4. Create the application
# 5. Copy the Client ID and Client Secret
#
LINEAR_CLIENT_ID=
LINEAR_CLIENT_SECRET=

# ==============================================================================
# Development Settings (Optional)
# ==============================================================================

# Enable debug logging for OAuth flows
# DEBUG_OAUTH=true

# Custom OAuth callback port (default: 49152)
# OAUTH_CALLBACK_PORT=49152
