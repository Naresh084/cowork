# Q10-07 Evidence

## Change Summary

- Added a new sidecar chaos module at `apps/desktop/src-sidecar/src/chaos/` with:
  - fault domains: `provider`, `network`, `storage`, `ipc`,
  - injectable modes: `throw`, `delay`, `timeout`, `drop`,
  - rule-scoped probability and max-activation controls,
  - per-domain/per-mode and per-rule activation telemetry.
- Added deterministic unit coverage for each fault domain/mode path and control behavior.

## Files Changed

- apps/desktop/src-sidecar/src/chaos/types.ts
- apps/desktop/src-sidecar/src/chaos/fault-injector.ts
- apps/desktop/src-sidecar/src/chaos/index.ts
- apps/desktop/src-sidecar/src/chaos/fault-injector.test.ts

## Verification Commands

```bash
pnpm --filter @gemini-cowork/sidecar test -- src/chaos/fault-injector.test.ts
pnpm --filter @gemini-cowork/sidecar typecheck
```

## Verification Output Summary

- Test result: `1 passed file`, `5 passed tests`.
- Typecheck result: pass (`tsc --noEmit`).

## Acceptance Mapping

- Plan acceptance criteria: chaos fault injection exists for provider/network/storage/IPC failure classes.
- Result: Completed for module-level validation. Fault injection primitives and deterministic tests now cover all required domains.

## Residual Risks

- End-to-end chaos campaign wiring (injector enabled inside full runtime execution loops) should remain on the roadmap for scenario-level reliability certification.
