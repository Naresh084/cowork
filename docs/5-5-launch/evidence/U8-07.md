# U8-07 Evidence

## Change Summary

- Added dedicated design token stylesheet at `apps/desktop/src/styles/design-tokens.css`.
- Introduced canonical tokens for:
  - typography hierarchy (display/title/body/caption/mono),
  - spacing and shape scales,
  - motion durations and easing curves,
  - semantic surface aliases.
- Added reusable typography and motion utility classes (`type-*`, `motion-*`).
- Wired token file into app bootstrap via `main.tsx` import.

## Files Changed

- apps/desktop/src/styles/design-tokens.css
- apps/desktop/src/main.tsx

## Verification Commands

```bash
pnpm --filter @cowork/desktop typecheck
pnpm --filter @cowork/sidecar typecheck
```

## Verification Output Summary

- Desktop and sidecar typechecks pass with token stylesheet integration.

## Acceptance Mapping

- Plan acceptance criteria: Introduce visual system tokens, motion rules, and typographic hierarchy.
- Result: Done.

## Residual Risks

- Full migration of all existing component-level hardcoded values to tokenized references remains iterative.

## Rollback Path

- Remove `design-tokens.css` import from `main.tsx` and delete token stylesheet.
