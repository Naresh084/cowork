# U8-08 Evidence

## Change Summary

- Added reusable UI primitives under `components/ui`:
  - `TimelineBadge`
  - `ReliabilityLabel`
  - `PolicyPill`
- Exported new primitives through `components/ui/index.ts`.
- Replaced ad-hoc badge styles in active surfaces:
  - `ChatView` run-status rail now uses `TimelineBadge` and `ReliabilityLabel`.
  - `MessageList` permission inline cards now use `PolicyPill`.

## Files Changed

- apps/desktop/src/components/ui/TimelineBadge.tsx
- apps/desktop/src/components/ui/ReliabilityLabel.tsx
- apps/desktop/src/components/ui/PolicyPill.tsx
- apps/desktop/src/components/ui/index.ts
- apps/desktop/src/components/chat/ChatView.tsx
- apps/desktop/src/components/chat/MessageList.tsx

## Verification Commands

```bash
pnpm --filter @cowork/desktop typecheck
pnpm --filter @cowork/sidecar typecheck
```

## Verification Output Summary

- Desktop and sidecar typechecks pass with shared primitive integration.

## Acceptance Mapping

- Plan acceptance criteria: New surfaces use shared UI primitives, not ad hoc styles.
- Result: Done.

## Residual Risks

- Additional legacy surfaces still using inline badge styles can be migrated incrementally to these shared primitives.

## Rollback Path

- Remove new primitive files/exports and restore prior inline badge markup in `ChatView` and `MessageList`.
