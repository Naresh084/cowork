# PH10-01 Evidence

## Validation Summary

- Executed deterministic full 600-scenario benchmark suite (`twin-track-core`) for three consecutive passes.
- Archived outputs:
  - `docs/5-5-launch/benchmark-runs/twin-track-core-3-pass.json`
  - `docs/5-5-launch/benchmark-runs/twin-track-core-3-pass.md`

## Verification Commands

```bash
cd apps/desktop/src-sidecar && pnpm exec tsx <<'TS'
import { BenchmarkRunner } from './src/benchmark/runner.ts';
import { BENCHMARK_SUITES } from './src/benchmark/suites/index.ts';
const runner = new BenchmarkRunner(BENCHMARK_SUITES);
await runner.runSuite('manual-pass-1', 'twin-track-core', 'release');
await runner.runSuite('manual-pass-2', 'twin-track-core', 'release');
await runner.runSuite('manual-pass-3', 'twin-track-core', 'release');
TS
```

## Acceptance Mapping

- Plan acceptance criteria: Run full 600-scenario benchmark.
- Result: Done.

