# Q10-08 Evidence

## Change Summary

- Added CI quality-gate workflow: `.github/workflows/quality-gates.yml`.
- Introduced benchmark regression gate with deterministic threshold test:
  - new sidecar test: `apps/desktop/src-sidecar/src/benchmark/runner.regression.test.ts`.
- Wired reliability regression gate to run critical Chromium E2E suites:
  - `e2e/reliability.spec.ts`,
  - `e2e/memory.spec.ts`,
  - `e2e/workflow.spec.ts`.
- Configured artifact upload on E2E failure for debugging.

## Files Changed

- .github/workflows/quality-gates.yml
- apps/desktop/src-sidecar/src/benchmark/runner.regression.test.ts

## Verification Commands

```bash
pnpm --filter @gemini-cowork/sidecar test -- src/benchmark/runner.regression.test.ts src/benchmark/suites/twin-track-core.test.ts
pnpm --filter @gemini-cowork/sidecar typecheck
pnpm --filter @gemini-cowork/desktop exec playwright test e2e/reliability.spec.ts e2e/memory.spec.ts e2e/workflow.spec.ts --project=chromium --config=playwright.reliability.local.config.ts
pnpm --filter @gemini-cowork/desktop typecheck
```

## Verification Output Summary

- Sidecar benchmark tests: pass (`3 passed`).
- Sidecar typecheck: pass.
- Reliability/memory/workflow E2E gate: pass (`11 passed`).
- Desktop typecheck: pass.

## Acceptance Mapping

- Plan acceptance criteria: CI blocks regressions on benchmark delta and reliability suites.
- Result: Completed. Added dedicated CI workflow with benchmark threshold gate + reliability E2E gate configured for PR/push checks.

## Residual Risks

- Benchmark thresholds are calibrated to current deterministic baseline; future score model changes should update test thresholds via explicit governance review.
